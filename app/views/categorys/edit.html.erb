
<h2>カテゴリー編集</h2>

  現在のカテゴリー名:<%= @category.category_name %>

  <%= form_with(model: @category, url: category_path, lacal: true) do |f| %>
    新しいカテゴリー名:<%= f.text_field :category_name, value: "", placeholder:"カテゴリー名" %>
    <%= f.submit "変更を保存する" %>
  <% end %>
